name: mlp

# Specify null as the number of positional encoding octaves if no positional encoding
# should be applied. The null becomes None in Python.

mlp:
  # positional_encoding_octaves: null
  positional_encoding_octaves: 6

  num_hidden_layers: 2
  d_hidden: 128
